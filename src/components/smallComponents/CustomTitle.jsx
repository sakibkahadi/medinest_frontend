

const CustomTitle = ({heading}) => {
    return (
        <div>
            <h1 className=" text-black font-bold text-2xl md:text-3xl  ">{heading}</h1>
        </div>
    );
};

export default CustomTitle;